{
  "name": "@komui/server",
  "version": "0.1.0",
  "private": true,
  "description": "KOMUI storefront backend and email worker for staging and production",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "start:email-worker": "node dist/emailWorker.js",
    "email:stage-acceptance": "node dist/emailStageAcceptance.js",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "pg": "^8.13.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=22"
  }
}
